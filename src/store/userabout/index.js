import { createUserTempId } from "@/utils/user"

const state = {
  userTempId: createUserTempId()
}

const mutations = {
}

const actions = {
}

const getters = {
}

export default {
  state,
  mutations,
  actions,
  getters
}
